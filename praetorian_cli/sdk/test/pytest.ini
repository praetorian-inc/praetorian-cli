[pytest]
addopts = -p no:cacheprovider
markers =
    coherence: these are tests on product workflows via the backend API
    cli: these are tests on the CLI interface
    speed: these are performance monitoring and heavy use tests
