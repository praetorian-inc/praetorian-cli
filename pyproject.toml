[build-system]
requires = [
    "setuptools>=54",
    "wheel"
]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
markers = [
    "cli: These are tests testing the CLI interface",
    "coherence: mark a test as a coherence test"
]
